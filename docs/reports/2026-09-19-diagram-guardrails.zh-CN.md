# 2026 年 9 月 19 日出图规划与学习文档更新

基线：v1.2.5 / 5a7cd33。此次是源码更新，不是新的桌面安装包发布。

## 改进内容

- 模型规划先检查模块对象形状、模块数量、短标签及内部组件预算；异常规划交给已有规则回退，不再因字符串模块继续进入后续处理而失败。
- 去掉为视觉复杂度默认添加注意力、奖励结构、公式和反馈箭头的提示词。
- 有来源支持的公式和内部组件可以同时进入绘图说明，避免已有子模块时把公式吞掉。
- 增加 22 章大白话项目手册，按真实入口讲清桌面结构、检索、PDF、RAG、模型路由、数据、出图、本地 Qwen 和 FTI 学习路径。

## 验证

在 backend 目录运行：

```text
python -m pytest tests/test_services/test_diagram_planner.py tests/test_services/test_diagram_pipeline.py tests/test_services/test_model_router.py -q --disable-warnings
```

结果：74 passed，3 个现有 Pydantic 弃用警告。其中规划新增 48 项，原有路线流程 14 项，模型路由 12 项。`git diff --check` 通过。

上述自动化测试使用离线替身，无收费 API 调用，没有修改用户模型配置、数据库或密钥。它们不验证实际出图画质。本地 Qwen 的独立实测结果另列于下方。

## 后续边界

结构校验不是论文忠实性证明。规则回退仍是建议骨架，架构与路线布局选择仍有启发式行为；SVG 与生图尚未统一内容结构，额外架构提炼仍需审查。后续优先增加模块与连接的来源、用户确认结构、统一渲染数据，再评估各生图提供商的真实质量。

## 本地 Qwen 独立验证

根据用户提供的 LLM-Twin 位置，确认已有 Qwen2.5-1.5B-Instruct，约 15.4 亿参数，Safetensors 主权重 3,087,467,144 字节，BF16 存储且未量化。文件 SHA-256 与项目下载记录相符；现有 Transformers 环境以 CUDA FP16 运行，未启用演示 LoRA，没有重新下载模型。

2026 年 9 月 19 日在 RTX 2060 6GB 上完成一次本地短问答。给定 ScholarNova 的四项能力，要求用一句话概括。模型加载耗时 11.13 秒，生成 25 tokens 耗时 6.40 秒，PyTorch 峰值显存分配 2.89GiB。返回内容准确概括了给定简介，未加入额外能力。测试进程已退出，未调用云 API、修改应用配置或留下常驻服务。此单样本不是稳定性能、长文分析或科研推理的评测。

现有 LLM-Twin 是 Gradio 入口，尚未接入 ScholarNova。后续需要兼容聊天接口、严格限定 loopback 的桌面地址校验，以及免 Key 自定义模型的产品帮助调用适配。首轮只接助手文字任务；不能让视觉、出图、向量任务继承这个纯文字模型。还应按真实 token 数管理上下文、预热模型，并验证各任务不同的等待预算。
